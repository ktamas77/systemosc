# Output modes (enable at least one)
OSC_ENABLED=true
HTTP_ENABLED=false

# OSC target configuration (used when OSC_ENABLED=true)
OSC_HOST=localhost
OSC_PORT=9877

# HTTP server configuration (used when HTTP_ENABLED=true)
HTTP_PORT=3000

# Interval in milliseconds (10000 = 10 seconds)
INTERVAL_MS=10000
